/** ADMIN layer — shared background-refresh cadence for admin data hooks.
 * Pages poll silently on this interval instead of requiring a manual Refresh
 * button click. */
export const ADMIN_POLL_INTERVAL_MS = 60000;
